# Dependencies
node_modules/
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build output
dist/
*.tsbuildinfo

# Secrets — NEVER commit tokens or per-account login dirs
*.token
*.oauth
.env
.env.*
secrets/
accounts/
.claude-*/

# Internal / operational — NEVER distribute (this repo is public-npm-distributed).
# Incident reports, topology notes, and operator-internal docs live outside the repo.
# See CLAUDE.md "Never commit — internal / operational detail".
docs/incidents/
*.internal.md
INTERNAL_*

# Packed artifacts / coverage / local Claude Code state
*.tgz
coverage/
.claude/

# Logs / OS
*.log
.DS_Store
npm-debug.log*

# Editor
.vscode/
.idea/

# Plugin Inspector output
reports/
